[build]
  base = "frontend"
  command = "npm ci && npm run build:prod"
  publish = "dist/scib-candidates-web/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
